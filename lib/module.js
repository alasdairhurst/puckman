function Module(name, config) {
	return {
	// search for module
		fetch: () => {
		},

		install: () => {
		// uninstall dependencies
		// npm install
		// relink dependencies
		},

		getPath: () => {
		},

		getLinkedModules: () => {
		}
	};
}
